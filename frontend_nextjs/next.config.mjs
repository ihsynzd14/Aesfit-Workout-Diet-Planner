/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['wger.de','wger.denull'],
  },
    reactStrictMode: true,
    swcMinify: true,
  }
  
  export default nextConfig